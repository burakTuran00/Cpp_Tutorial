#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double x = 3.14;
    double y = 4;
    double z;

    z = max(x, y);

    z = min(x, y);

    z = pow(2, 3);

    z = sqrt(9);

    z = abs(-5);

    z = round(x);

    z = ceil(x); // up

    z = floor(x); // down

    return 0;
}